{
  "name": "aws-xray-sdk-koa",
  "version": "1.0.0",
  "description": "AWS X-Ray Middleware for Koa (Javascript)",
  "main": "index.js",
  "engines": {
    "node": ">= 7.10"
  },
  "author": "David G. Hong",
  "license": "MIT",
  "peerDependencies": {
    "aws-xray-sdk-core": "^1.1.2"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^4.0.2",
    "prettier": "^1.5.3"
  },
  "scripts": {
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.js": [
      "prettier --use-tabs --single-quote --trailing-comma es5 --write",
      "git add"
    ]
  },
  "dependencies": {
    "aws-xray-sdk-core": "^1.1.2"
  }
}
